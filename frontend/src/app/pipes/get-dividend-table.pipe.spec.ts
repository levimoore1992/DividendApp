import { GetDividendTablePipe } from './get-dividend-table.pipe';

describe('GetDividendTablePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDividendTablePipe();
    expect(pipe).toBeTruthy();
  });
});
